import { DictionaryToSelectPipe } from './dictionary-to-select.pipe';

describe('DictionaryToSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryToSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
