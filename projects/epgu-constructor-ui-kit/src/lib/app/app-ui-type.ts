import { AppRoutingComponentMap } from './app-routing/app-routing';
import { AppNavigationRuleMap } from './app-navigation-rule/app-navigation-rule';

export interface AppUiConfig {
  appRoutingComponentMap: AppRoutingComponentMap,
  appNavigationRuleMap: AppNavigationRuleMap,
}
