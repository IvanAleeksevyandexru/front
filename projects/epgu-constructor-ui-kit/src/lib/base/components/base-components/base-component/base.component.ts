import { IconType } from '../../icon-component/IconType';
import { IconColor } from '../../icon-component/IconColor';

export class BaseComponent {
  public iconType = IconType;
  public iconColor = IconColor;
}
